<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.shots.squads_and_shots">
    
    <uses-permission android:name="usesCleartextTraffic"/>
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name="com.shots.SquadsAndShots"
        android:theme="@style/PsMaterial.AppTheme.Main">
        <activity android:name="com.shots.squads_and_shots.presentation.homePage.HomePage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".presentation.leadHoldingLobby.LeadHoldingLobby"/>
        <activity android:name=".presentation.mainGameRoom.MainGameRoom"/>
    </application>

</manifest>