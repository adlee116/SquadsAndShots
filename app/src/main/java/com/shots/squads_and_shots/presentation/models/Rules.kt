package com.shots.squads_and_shots.presentation.models

class Rules {
    var generalRules: List<GeneralRule> = emptyList()
    var secretTasks: List<SecretTasks> = emptyList()
    var nominatedRules: List<NominatedRule> = emptyList()
}
